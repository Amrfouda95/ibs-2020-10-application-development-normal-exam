class Fish:
    def __init__(self, name, weight, color):
        self.name = name
        self.weight = weight
        self.color = color

    def status(fish):
        if (fish.name == "Clownfish"):
            print("Fish: {0}, Weight: {1}, Color: {2}".format(fish.name, fish.weight, fish.color))

        if (fish.name == "Tang"):
            print("Fish: {0}, Weight: {1}, Color: {2}".format(fish.name, fish.weight, fish.color))

        if (fish.name == "Kong"):
            print("Fish: {0}, Weight: {1}, Color: {2}".format(fish.name, fish.weight, fish.color))

    def feed(fish):
        if (fish.name == "Clownfish"):
            return 1
        if (fish.name == "Tang"):
            return 1
        if (fish.name == "Kong"):
            return 2

